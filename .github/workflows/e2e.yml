name: E2E

on:
  workflow_call:
    inputs:
      test_dirs:
        required: true
        type: string

jobs:
  cancel-prev:
    name: Cancel Prev
    runs-on: ubuntu-20.04
    timeout-minutes: 3
    steps:
      - name: Cancel Prev
        uses: styfle/cancel-workflow-action@0.9.1
        with:
          access_token: ${{ github.token }}
  test:
    name: Test
    needs: cancel-prev
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-20.04, windows-2019]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 14
      - name: Install, build and prepare fixtures
        run: |
          yarn
          yarn fixtures install ${{ inputs.test_dirs }}
      - name: Test end to end cases
        run: yarn e2e ${{ inputs.test_dirs }}
